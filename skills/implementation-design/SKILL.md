---
name: implementation-design
description: deprecated — /po로 통합됨. standalone 호출 시 /po mode=build 사용.
---

# Implementation Design (Deprecated)

이 skill은 `/po`로 통합되었다.

- control-flow 내부 build 단계: `Agent(subagent_type="po")` 사용
- standalone 직접 호출: `/po mode=build` 사용
