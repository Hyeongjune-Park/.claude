---
name: planning
description: deprecated — /po로 통합됨. standalone 호출 시 /po mode=plan 사용.
---

# Planning (Deprecated)

이 skill은 `/po`로 통합되었다.

- control-flow 내부 planning 단계: `Agent(subagent_type="po")` 사용
- standalone 직접 호출: `/po mode=plan` 사용
